import test from 'node:test'; import assert from 'node:assert/strict'; import { compose } from '../src/core/compose.mjs';
test('commercial intelligence resolves scope, ownership and explainable estimate',()=>{const p=compose('Premium bilingual company website with CMS, SEO, analytics, custom animation and CRM integration.');assert.equal(p.version,'8.0.0');assert.ok(p.commercial.complexity>=30);assert.ok(['GROW','PERFORM','CUSTOM'].includes(p.commercial.tier));assert.equal(p.commercial.estimate.currency,'CZK');assert.equal(p.commercial.estimate.nonBinding,true);assert.ok(p.commercial.ownership.includes('source code'));assert.equal(p.commercial.promise,'No WEBFORGE lock-in');});
test('commercial intelligence escalates application complexity',()=>{const simple=compose('Simple local barber website with contact and opening hours.');const complex=compose('Marketplace web app with payments, checkout, API integrations, analytics, CMS, multilingual content and custom interactive design.');assert.ok(complex.commercial.complexity>simple.commercial.complexity);assert.ok(['PERFORM','CUSTOM'].includes(complex.commercial.tier));});
