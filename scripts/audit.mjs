import fs from 'node:fs';import {registrySummary} from '../src/core/composition-registry.mjs';
const pkg=JSON.parse(fs.readFileSync('package.json'));const comps=JSON.parse(fs.readFileSync('src/registries/components.json'));const tools=JSON.parse(fs.readFileSync('src/registries/tools.json'));const registry=registrySummary();console.log(JSON.stringify({package:pkg.name,version:pkg.version,dependencies:0,components:comps.length,tools:tools.length,compositionRegistry:registry.counts,quality:registry.quality,supplyChain:{runtimeDependencies:'NONE',externalAdapters:'CONDITIONAL/UNVERIFIED',policy:'FAIL-CLOSED'}},null,2));
